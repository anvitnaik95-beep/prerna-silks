# Render.com Deployment Blueprint
# This file configures automatic deployment on Render.com (free tier)

services:
  # Full-stack Web Service (Backend + Frontend served together)
  - type: web
    name: prerna-silks
    runtime: node
    plan: free
    buildCommand: cd client && npm install && npx vite build && cd ../server && npm install
    startCommand: cd server && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false  # Set manually in Render dashboard
      - key: JWT_SECRET
        sync: false
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false
      - key: ADMIN_PHONE
        value: "7019461619"
      - key: ADMIN_EMAIL
        value: "anvitnaik95@gmail.com"
      - key: ADMIN_UPI
        value: "7019461619@ptyes"
      - key: SENDGRID_API_KEY
        value: "REPLACE_WITH_YOUR_SENDGRID_API_KEY"
      - key: SENDGRID_FROM
        value: "anvitnaik95@gmail.com"
      - key: WA_PHONE_NUMBER_ID
        sync: false  # Set in Render dashboard
      - key: WA_ACCESS_TOKEN
        sync: false
