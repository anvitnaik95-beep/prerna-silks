{
  "name": "prerna-silks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npx vite build && cd ../server && npm install",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
